package STRING;

public class NewPalin {
    public String long(
    String s){
        int start =0, end=0;
        for(int i=0;i<s.length();i++){
            int l1=
            int l2 = expandFromCenter(s,i,i);
            int len = Math.max(l1, l2);
            if(end-start<len)
        }


    }

}
